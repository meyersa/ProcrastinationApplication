// Package when an error happens, calls the ErrorGUI, takes string error (e.toString())

package main;

import java.io.IOException;

public class Error {

	public static void createErrorLog(Exception e) {
		// Create Error file in the cache directory and add e onto the file seperated by space probably 
		
	}
	
	public static void createErrorGUI(Exception e) {
		// Creates critical crash gui using ErrorGUI in gui package 
		
	}
}
